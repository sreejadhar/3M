apiVersion: apps/v1
kind: Deployment
metadata:
  name: kg-api
  namespace: datananite
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kg-api
  template:
    metadata:
      labels:
        app: kg-api
    spec:
      initContainers:
      - name: fix-permissions
        image: busybox
        command: ["sh", "-c", "mkdir -p /data/reports && chmod -R 777 /data/reports"]
        volumeMounts:
        - name: reports-data
          mountPath: /data/reports
      containers:
      - name: kg-api
        image: gcr.io/cog01k24f1ea555zdv7ynzthxanz5/datananite-kg:latest
        ports:
        - containerPort: 8002
        envFrom:
        - configMapRef:
            name: datananite-config
        - secretRef:
            name: datananite-secrets
        volumeMounts:
        - name: reports-data
          mountPath: /data/reports
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 90
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 90
          periodSeconds: 10
      volumes:
      - name: reports-data
        persistentVolumeClaim:
          claimName: kg-api-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: kg-api-service
  namespace: datananite
spec:
  selector:
    app: kg-api
  ports:
  - port: 8002
    targetPort: 8002
  type: ClusterIP

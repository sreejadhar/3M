apiVersion: apps/v1
kind: Deployment
metadata:
  name: dialog-api
  namespace: datananite2
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: dialog-api
  template:
    metadata:
      labels:
        app: dialog-api
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: cloud.google.com/gke-nodepool
                operator: In
                values:
                - datananite-pool
      containers:
      - name: dialog-api
        image: gcr.io/cog01k24f1ea555zdv7ynzthxanz5/datananite-dialog:latest
        ports:
        - containerPort: 8003
        envFrom:
        - configMapRef:
            name: datananite-config
        - secretRef:
            name: datananite-secrets
        volumeMounts:
        - name: shared-data
          mountPath: /data
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "3Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 60
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 60
          periodSeconds: 10
      volumes:
      - name: shared-data
        persistentVolumeClaim:
          claimName: datananite2-shared-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: dialog-api-service
  namespace: datananite2
  annotations:
    cloud.google.com/neg: '{"ingress":true}'
spec:
  selector:
    app: dialog-api
  ports:
  - port: 8003
    targetPort: 8003
  type: ClusterIP

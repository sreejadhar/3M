# DataChat Orchestrator — dependencies
fastapi>=0.111.0
anthropic[vertex,bedrock]>=0.34.0      # KPI formula compiler (kpi_store.compile_formula)
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
httpx>=0.27.0          # async HTTP calls to the other microservices
boto3>=1.34.0          # Bedrock (assumed cross-account role — aws_auth.py/llm_client.py)
requests>=2.31.0       # Neptune openCypher HTTPS Data API (neptune_store.py, via kg_store)
python-multipart>=0.0.9  # file upload support
numpy>=1.26.0          # GraphRAG cosine similarity
scikit-learn>=1.4.0    # TF-IDF embedding backend (optional but recommended)
# sentence-transformers>=2.7.0  # best-quality embeddings (optional, requires torch)
openpyxl>=3.1.0               # Excel export (multi-tab workbook + charts)
matplotlib>=3.7.0             # chart rendering to PNG for Excel embedding
Pillow>=10.0.0                # image handling for openpyxl XlImage embedding
bcrypt>=4.0.0                 # password hashing for auth
PyJWT>=2.8.0                  # JWT token signing/verification
sqlglot>=25.0.0                # SQL parsing for dialog_agent.nodes.sql_identifier_resolver,
                                # transitively imported via dialog_agent.nodes.execute_node
                                # (cross-source bridge inference sample queries)
psycopg2-binary>=2.9.9         # PostgreSQL — metadata_catalog/kg_store/kpi_store/glossary_registry
                                # switch to Postgres when APP_ENV=production + KG_POSTGRES_DSN are set
rdflib>=7.0.0                  # business_ontology — SKOS+OWL graph generation/serialization
snowflake-connector-python>=3.6.0  # dialog_agent.nodes.execute_node._run_snowflake / snowflake_auth.py —
                                    # chat-ui runs the full dialog pipeline in-process (see orchestrator_api.py)

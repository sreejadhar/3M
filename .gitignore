__pycache__/
*.py[cod]
*.pyo
.env
data/
reports/
*.db
*.sqlite
*.sqlite3
.DS_Store

# Node / Vite build artifacts (dist/ is intentionally tracked so 8005/8006 serve React)
node_modules/
.vite/

# Local dev logs & scratch
logs/
*.log

# One-time embedding-model download artifact (uploaded to the shared PVC,
# not committed — see scripts/bootstrap_embedding_model_pvc.md)
/models/

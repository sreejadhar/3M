# Knowledge Graph Agent API service dependencies

# FastAPI service layer
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Ontology parsing
rdflib>=7.0.0

# LangGraph pipeline
langgraph>=0.2.0
langchain-core>=0.3.0

# Anthropic LLM (profile_node)
anthropic[bedrock]>=0.34.0

# AWS (Bedrock via assumed cross-account role, Neptune KG storage — see
# aws_auth.py/llm_client.py and neptune_store.py)
boto3>=1.34.0
requests>=2.31.0

# PostgreSQL — kg_store.py source registry (RDS, credentials from Secrets
# Manager — see pg_secrets.py)
psycopg2-binary>=2.9.9

# GraphRAG embedding (embed_node)
numpy>=1.26.0                          # vector math (required)
# sentence-transformers>=3.0.0         # best-quality local backend (optional, 384 dims)
# openai>=1.0.0                        # OpenAI backend (optional, 1536 dims, needs OPENAI_API_KEY)

# Dialog with Data Agent — API service dependencies
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
anthropic>=0.28.0
langgraph>=0.1.0
langchain-core>=0.2.0

# File-based sources (CSV / Excel) — sqlite3 is stdlib
pandas>=2.0.0
openpyxl>=3.1.0

# GraphRAG in-memory retrieval
numpy>=1.26.0                          # vector math for cosine similarity (required)
scikit-learn>=1.4.0                    # TF-IDF embedding backend (recommended)
# sentence-transformers>=3.0.0         # best-quality local embedding backend (optional)
# openai>=1.0.0                        # OpenAI embedding backend (optional, needs OPENAI_API_KEY)

# Database drivers (optional — only the target DB driver is needed at runtime)
psycopg2-binary>=2.9.9        # PostgreSQL / Redshift
# cx_Oracle>=8.3.0            # Oracle (install manually if needed)
# pyodbc>=5.0.0               # SQL Server (install manually if needed)
# google-cloud-bigquery>=3.0  # BigQuery (install manually if needed)

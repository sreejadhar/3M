# Unstructured Data Intelligence Agent — API service dependencies
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
httpx>=0.27.0
anthropic[vertex]>=0.28.0

# Document parsers
pdfplumber>=0.11.0            # PDF text extraction (digital PDFs)
python-docx>=1.1.0            # DOCX files
python-pptx>=0.6.23           # PPTX files
openpyxl>=3.1.0               # XLSX files (sheet names + named ranges only)
beautifulsoup4>=4.12.0        # HTML files
lxml>=5.0.0                   # fast HTML parser backend for BeautifulSoup

# OCR (optional — only needed for scanned PDFs)
# pdf2image>=1.16.0           # converts PDF pages to images (requires poppler)
# pytesseract>=0.3.10         # OCR wrapper for Tesseract (requires tesseract-ocr system package)

# AWS S3 connector
boto3>=1.34.0

# Google Drive connector
google-api-python-client>=2.100.0
google-auth>=2.22.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.2.0

# SharePoint / OneDrive connector (Microsoft Graph API)
msal>=1.28.0

# SQLite is stdlib — no extra driver needed

# Semantic embeddings (embedder.py) — optional, auto-detected at runtime.
# Neither installed → embedding step is skipped and doc similarity falls
# back to Jaccard over topic sets.
# sentence-transformers>=3.0.0   # local backend (default when installed), 384 dims
# openai>=1.0.0                  # OpenAI backend, 1536 dims, needs OPENAI_API_KEY

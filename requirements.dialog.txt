# Dialog with Data Agent — API service dependencies
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
anthropic[vertex]>=0.28.0
langgraph>=0.1.0
langchain-core>=0.2.0

# File-based sources (CSV / Excel) — sqlite3 is stdlib
pandas>=2.0.0
openpyxl>=3.1.0

# GraphRAG in-memory retrieval
numpy>=1.26.0                          # vector math for cosine similarity (required)
scikit-learn>=1.4.0                    # TF-IDF embedding backend (recommended)
# sentence-transformers>=3.0.0         # best-quality local embedding backend (optional)
# openai>=1.0.0                        # OpenAI embedding backend (optional, needs OPENAI_API_KEY)

# Database drivers (install only the driver for your target DB)
psycopg2-binary>=2.9.9        # PostgreSQL / Redshift (always included — no side-effects)
pymssql>=2.2.0                 # SQL Server — pure-Python wheel, no ODBC driver required
# pyodbc>=5.0.0                # SQL Server alternative — needs unixODBC + ODBC Driver 18 (enterprise setups)

# Oracle — uncomment one:
# oracledb>=2.0.0              # Recommended: thin mode, no Instant Client required
# cx_Oracle>=8.3.0             # Legacy: requires Oracle Instant Client

# BigQuery — uncomment both:
# google-cloud-bigquery>=3.0.0
# google-auth>=2.0.0

# Teradata:
# teradatasql>=20.0.0

# Databricks SQL (REST transport):
# databricks-sql-connector>=3.0.0

# Delta Lake local / PySpark:
# pyspark>=3.4.0
# delta-spark>=3.0.0

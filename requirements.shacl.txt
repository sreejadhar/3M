# SHACL Validation API service dependencies

# FastAPI service layer
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# SHACL validation engine
pyshacl>=0.26.0

# RDF/OWL parsing
rdflib>=7.0.0

# LangGraph pipeline
langgraph>=0.2.0
langchain-core>=0.3.0

# Async HTTP (fetch ontology from Ontology API)
httpx>=0.27.0
